## What changed and why


## Checklist
- [ ] Tests added/updated for new logic (`cd apps/api && pytest tests/`)
- [ ] `docs/IMPLEMENTATION_STATUS.md` updated if this changes what's implemented vs. planned
- [ ] Verified against the running stack (`docker compose up --build`), not just unit tests, if this touches API routes, migrations, or the frontend
- [ ] No secrets, API keys, or `.env` files committed

## Related issue
Closes #
